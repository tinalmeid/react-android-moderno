const React = require("react");

// Simple icon stub used during tests; avoids pulling native icon fonts.
const Ionicons = (props = {}) => React.createElement("Ionicons", props);

module.exports = {
  __esModule: true,
  Ionicons,
  default: { Ionicons },
};
