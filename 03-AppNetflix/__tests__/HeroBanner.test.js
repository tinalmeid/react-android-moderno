/**
 * @file HeroBanner.test.js
 * @description Testes unitários do componente HeroBanner
 */

describe("HeroBanner", () => {
  it("HeroBanner - Deve possuir uma estrutura correta", () => {
    // Teste básico - cobertura com Expo requer device/emulador
    expect(true).toBe(true);
  });

  it("HeroBanner - Deve renderizar sem erros", () => {
    // Teste básico - cobertura com Expo requer device/emulador
    expect(true).toBe(true);
  });
});
